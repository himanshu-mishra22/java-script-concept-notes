// if we need to export them into next class we need to use export keyword before the variable name


export const PI = 3.141592;

export function getCircum(radius){
    return 2*PI*radius;
}

export function getArea(radius){
    return PI*(radius**2);
}

